version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DEBUG=False
    volumes:
      - ./config.json:/app/config.json
      - ./browser_context.json:/app/browser_context.json
    restart: unless-stopped
